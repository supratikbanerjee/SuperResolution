name: _0001_x2_DBPNS
is_train: false
model: cnn
device: cuda:1

dataset:
  read: ram # disk
  scale: 2
  test_1:
    name: Set5
    data_location: data/datasets/Set5/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1

#### network structures
network_G:
  model: DBPNS
  num_features: 64 #DBPN
  in_channels: 3
  out_channels: 18 #DBPN
  num_blocks: 2


#### path
path:
  experiments: trained_models/experiments/
  pretrain_model_G: trained_models/1000_DBPNS_x2_netG.pth
