name: RDCAN_x2_100Epoch_RNN
is_train: false
model: cnn
device: cuda

dataset:
  scale: 2
  test_1:
    name: Set5
    data_location: data/datasets/Set5/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1
  test_2:  # the 2st test dataset
    name: Set14
    data_location: data/datasets/Set14/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1
  test_3:  # the 2st test dataset
    name: BSDS100
    data_location: data/datasets/BSDS300/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1
  test_4:  # the 2st test dataset
    name: DIV2K
    data_location: data/datasets/DIV2K/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1

#### network structures
network_G:
  model: RDCAN
  num_features: 32
  in_channels: 3
  out_channels: 3
  num_steps: 4 # later use
  num_groups: 3
  train: false


#### path
path:
  experiments: trained_models/experiments/
  pretrain_model_G: trained_models/100_RDCAN_x2_netG_CA.pth
