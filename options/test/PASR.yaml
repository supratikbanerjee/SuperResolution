name: PASR_01
is_train: false
model: cnn
device: cuda

dataset:
  scale: 2
  test_1:
    name: Set5
    data_location: data/datasets/Render4K/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1

#### network structures
network_G:
  model: PASR
  num_features: 16
  in_channels: 3
  out_channels: 3
  num_steps: 1
  num_groups: 1

#### path
path:
  experiments: trained_models/experiments/
  pretrain_model_G: trained_models/1000_PASR_x2_netG.pth
