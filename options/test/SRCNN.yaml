name: SRCNN_x2_100Epoch
is_train: false
model: cnn
device: cuda

dataset:
  scale: 2
  test_1:
    name: Set5
    data_location: data/datasets/Set5/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1
  test_2:  # the 2st test dataset
    name: Set14
    data_location: data/datasets/Set14/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1
  test_3:  # the 2st test dataset
    name: BSDS100
    data_location: data/datasets/BSDS300/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1
  test_4:  # the 2st test dataset
    name: DIV2K
    data_location: data/datasets/DIV2K/test/
    shuffle: false
    n_workers: 1  # per GPU
    batch_size: 1
    repeat: 1

#### network structures
network_G:
  model: SRCNN

#### path
path:
  experiments: trained_models/experiments/
  pretrain_model_G: trained_models/100_SRCNN_x2_netG.pth
